{
  "id": 162,
  "slug": "find-peak-element",
  "title": "Find Peak Element",
  "difficulty": "Medium",
  "topics": [
    "Array",
    "Binary Search"
  ],
  "url": "https://leetcode.com/problems/find-peak-element/",
  "comparison": "AnyValid",
  "folder": "Searching",
  "className": "FindPeakElement",
  "statement": "A peak is any element strictly greater than both its neighbours, with positions outside the array treated as negative infinity. Return the index of ANY peak. Several may exist and any is accepted. Must run in logarithmic time.",
  "constraints": [
    "1 <= nums.Length <= 1000",
    "neighbouring values are never equal"
  ],
  "hints": [
    "Logarithmic time on an unsorted array sounds impossible, but you are not searching for a specific value.",
    "Look at the middle element and its right neighbour. If the slope is going up, a peak must exist somewhere to the right; if down, one exists at mid or to its left.",
    "That is enough to halve the range every step, because the array boundaries act as negative infinity and guarantee a peak in whichever half you keep."
  ],
  "complexity": "O(log n) time",
  "signature": "int Solve(int[] nums)",
  "extraMembers": [
    "/// <summary>Harness plumbing: any index that beats both neighbours is accepted.</summary>\npublic static bool Validate(object?[] args, object? actual, JsonElement expected)\n{\n    int[] nums = (int[])args[0]!;\n    if (actual is not int i || i < 0 || i >= nums.Length) return false;\n    return (i == 0 || nums[i] > nums[i - 1])\n        && (i == nums.Length - 1 || nums[i] > nums[i + 1]);\n}"
  ],
  "cases": [
    {
      "name": "[1,2,3,1] (index 2)",
      "args": [
        [
          1,
          2,
          3,
          1
        ]
      ],
      "expected": 2
    },
    {
      "name": "[1,2,1,3,5,6,4] (1 or 5)",
      "args": [
        [
          1,
          2,
          1,
          3,
          5,
          6,
          4
        ]
      ],
      "expected": 5
    },
    {
      "args": [
        [
          1
        ]
      ],
      "expected": 0
    },
    {
      "args": [
        [
          2,
          1
        ]
      ],
      "expected": 0
    }
  ]
}