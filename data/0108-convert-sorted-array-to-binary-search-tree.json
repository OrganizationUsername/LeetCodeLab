{
  "id": 108,
  "slug": "convert-sorted-array-to-binary-search-tree",
  "title": "Convert Sorted Array to Binary Search Tree",
  "difficulty": "Easy",
  "topics": [
    "Array",
    "Divide and Conquer",
    "Tree",
    "BST",
    "Binary Tree"
  ],
  "url": "https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/",
  "comparison": "AnyValid",
  "folder": "Trees",
  "className": "SortedArrayToBst",
  "statement": "The array is sorted ascending. Build a height-balanced binary search tree from it. Several trees are valid and any height-balanced one is accepted.",
  "constraints": [
    "1 <= nums.Length <= 10^4",
    "nums is sorted strictly ascending"
  ],
  "hints": [
    "Balance comes from splitting evenly, so make the MIDDLE element the root.",
    "Everything left of it becomes the left subtree, everything right becomes the right subtree, recursively.",
    "Both the lower and the upper middle are acceptable for an even-length slice, which is why several answers pass."
  ],
  "complexity": "O(n) time, O(log n) space",
  "signature": "TreeNode? Solve(int[] nums)",
  "extraMembers": [
    "/// <summary>\n/// Harness plumbing: many height-balanced BSTs are correct, so this checks the two\n/// properties instead of comparing against one particular tree.\n/// </summary>\npublic static bool Validate(object?[] args, object? actual, JsonElement expected)\n{\n    int[] nums = (int[])args[0]!;\n    TreeNode? root = actual as TreeNode;\n\n    List<int> inorder = [];\n    void Walk(TreeNode? node)\n    {\n        if (node is null) return;\n        Walk(node.left);\n        inorder.Add(node.val);\n        Walk(node.right);\n    }\n    Walk(root);\n    if (!inorder.SequenceEqual(nums)) return false;\n\n    static int Height(TreeNode? node)\n    {\n        if (node is null) return 0;\n        int left = Height(node.left), right = Height(node.right);\n        if (left < 0 || right < 0 || Math.Abs(left - right) > 1) return -1;\n        return 1 + Math.Max(left, right);\n    }\n    return Height(root) >= 0;\n}"
  ],
  "cases": [
    {
      "args": [
        [
          -10,
          -3,
          0,
          5,
          9
        ]
      ],
      "expected": [
        0,
        -3,
        9,
        -10,
        null,
        5
      ]
    },
    {
      "args": [
        [
          1,
          3
        ]
      ],
      "expected": [
        3,
        1
      ]
    },
    {
      "args": [
        [
          1
        ]
      ],
      "expected": [
        1
      ]
    },
    {
      "name": "two values",
      "args": [
        [
          1,
          2
        ]
      ],
      "expected": [
        2,
        1
      ]
    },
    {
      "name": "three values",
      "args": [
        [
          1,
          2,
          3
        ]
      ],
      "expected": [
        2,
        1,
        3
      ]
    },
    {
      "name": "seven values make a perfect tree",
      "args": [
        [
          1,
          2,
          3,
          4,
          5,
          6,
          7
        ]
      ],
      "expected": [
        4,
        2,
        6,
        1,
        3,
        5,
        7
      ]
    },
    {
      "name": "four values",
      "args": [
        [
          -1,
          0,
          1,
          2
        ]
      ],
      "expected": [
        0,
        -1,
        2,
        null,
        null,
        1
      ]
    }
  ]
}
