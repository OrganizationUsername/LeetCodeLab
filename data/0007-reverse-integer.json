{
  "id": 7,
  "slug": "reverse-integer",
  "title": "Reverse Integer",
  "difficulty": "Medium",
  "topics": [
    "Math"
  ],
  "url": "https://leetcode.com/problems/reverse-integer/",
  "comparison": "Exact",
  "folder": "Numbers",
  "className": "ReverseInteger",
  "signature": "int Solve(int x)",
  "statement": "Reverse the digits of a signed 32-bit integer, keeping the sign. If the reversed value falls outside the signed 32-bit range, return 0 instead. You may not use a 64-bit type to dodge the check.",
  "constraints": [
    "-2^31 <= x <= 2^31 - 1"
  ],
  "hints": [
    "Peel digits with % 10 and / 10. In C# these already behave correctly for negatives, so the sign takes care of itself.",
    "The trap is detecting overflow BEFORE it happens.",
    "Before doing result = result * 10 + digit, check whether result already exceeds int.MaxValue / 10, and handle the boundary digit case."
  ],
  "complexity": "O(number of digits) time",
  "cases": [
    {
      "args": [
        123
      ],
      "expected": 321
    },
    {
      "args": [
        -123
      ],
      "expected": -321
    },
    {
      "args": [
        120
      ],
      "expected": 21
    },
    {
      "args": [
        1534236469
      ],
      "expected": 0
    },
    {
      "args": [
        0
      ],
      "expected": 0
    }
  ]
}