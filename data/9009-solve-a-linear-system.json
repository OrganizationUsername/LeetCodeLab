{
  "id": 9009,
  "slug": "solve-a-linear-system",
  "title": "Solve a Linear System",
  "difficulty": "Medium",
  "topics": ["Original", "Numerics", "Matrix"],
  "url": "",
  "comparison": "FloatTolerance",
  "folder": "Numerics",
  "className": "SolveALinearSystem",
  "signature": "double[] Solve(double[][] a, double[] b)",
  "statement": "Solve A x = b for x by Gaussian elimination. A is square and non-singular. Return x. This is the kernel every direct finite element solver is built on, so write it the way one would be written rather than inverting the matrix.",
  "constraints": [
    "1 <= n <= 100, A is n by n and b has length n",
    "A is non-singular, but a zero may still turn up in a pivot position",
    "you may modify the arrays you are given",
    "answers are checked to 1e-5"
  ],
  "hints": [
    "Two stages: eliminate below the diagonal to leave an upper-triangular system, then back-substitute from the last unknown upwards.",
    "The textbook version divides by a[k][k], which fails outright when that entry is zero and loses precision when it is merely small compared to what is below it.",
    "Partial pivoting fixes both: before eliminating column k, find the row at or below k with the largest absolute entry in that column and swap it up. Swap the matching entry of b too, which is the step people forget. Never invert A - inversion is slower and less accurate than elimination."
  ],
  "complexity": "O(n^3) time, O(1) extra space",
  "cases": [
    { "name": "2x2",
      "args": [[[2,1],[1,3]], [4,7]], "expected": [1,2] },
    { "name": "zero in the first pivot: needs a row swap",
      "args": [[[0,1],[1,0]], [2,3]], "expected": [3,2] },
    { "name": "symmetric, stiffness-like",
      "args": [[[4,-2,1],[-2,4,-2],[1,-2,4]], [3,0,9]], "expected": [1,2,3] },
    { "name": "1x1",
      "args": [[[5]], [10]], "expected": [2] },
    { "name": "3x3 with a negative solution",
      "args": [[[1,1,1],[0,2,5],[2,5,-1]], [6,-4,27]], "expected": [5,3,-2] }
  ]
}
